const lamp_icon = document.querySelectorAll(
  ".services__content-floor-lamps-icon"
);
lamp_icon.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});
const ceiling_lamp = document.querySelectorAll(
  ".services__content-ceiling-fixtures-icon"
);
ceiling_lamp.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});
const wall_sconces = document.querySelectorAll(
  ".services__content-wall-sconces-icon"
);
wall_sconces.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});
const wall_mounted = document.querySelectorAll(
  ".services__content-wall-mounted-icon"
);
wall_mounted.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});
const kitchen_island = document.querySelectorAll(
  ".services__content-kitchen-island-icon"
);
kitchen_island.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});
const modular_light = document.querySelectorAll(
  ".services__content-modular-lighting-icon"
);
modular_light.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});
const led_light = document.querySelectorAll(
  ".services__content-led-lighting-icon"
);
led_light.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});
const smart_light = document.querySelectorAll(
  ".services__content-smart-lighting-icon"
);
smart_light.forEach((icon) => {
  icon.addEventListener("mouseenter", () => {
    icon.classList.add("hover-animate");
  });

  icon.addEventListener("mouseleave", () => {
    icon.classList.remove("hover-animate");
  });
});

// interactive lighting mode //
const ceiling_catalog = document.querySelectorAll(
  ".services__content-ceiling-fixtures-catalog"
);

ceiling_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});

const floor_catalog = document.querySelectorAll(
  ".services__content-floor-lamps-catalog"
);

floor_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});

const wall_catalog = document.querySelectorAll(
  ".services__content-wall-sconces-catalog"
);

wall_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});
const mounted_catalog = document.querySelectorAll(
  ".services__content-wall-mounted-catalog"
);

mounted_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});
const kitchen_catalog = document.querySelectorAll(
  ".services__content-kitchen-island-catalog"
);

kitchen_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});
const modular_catalog = document.querySelectorAll(
  ".services__content-modular-lighting-catalog"
);

modular_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});

const led_catalog = document.querySelectorAll(
  ".services__content-led-lighting-catalog"
);

led_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});
const smart_catalog = document.querySelectorAll(
  ".services__content-smart-lighting-catalog"
);

smart_catalog.forEach((option) => {
  option.addEventListener("mouseenter", () => {
    option.classList.add("glow");
  });

  option.addEventListener("mouseleave", () => {
    option.classList.remove("glow");
  });
});
